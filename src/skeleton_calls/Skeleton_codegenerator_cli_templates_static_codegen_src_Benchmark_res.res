/* Auto-generated from codegenerator/cli/templates/static/codegen/src/Benchmark.res. */
let touch_all = () => {
  // Throttler.make
  let _Throttler_make = (Obj.magic(Throttler.make): unit => unit)
  ignore(_Throttler_make())
  // Throttler.schedule
  let _Throttler_schedule = (Obj.magic(Throttler.schedule): unit => unit)
  ignore(_Throttler_schedule())
  // Utils.magic
  let _Utils_magic = (Obj.magic(Utils.magic): unit => unit)
  ignore(_Utils_magic())
}
let _ = touch_all()
