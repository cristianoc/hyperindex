/* Auto-generated from codegenerator/cli/templates/static/codegen/src/LoadLayer.res. */
let touch_all = () => {
  // Promise.all
  let _Promise_all = (Obj.magic(Promise.all): unit => unit)
  ignore(_Promise_all())
  // Promise.make
  let _Promise_make = (Obj.magic(Promise.make): unit => unit)
  ignore(_Promise_make())
  // Promise.resolve
  let _Promise_resolve = (Obj.magic(Promise.resolve): unit => unit)
  ignore(_Promise_resolve())
  // Promise.then
  let _Promise_then = (Obj.magic(Promise.then): unit => unit)
  ignore(_Promise_then())
  // Utils.magic
  let _Utils_magic = (Obj.magic(Utils.magic): unit => unit)
  ignore(_Utils_magic())
}
let _ = touch_all()
