/* Auto-generated from codegenerator/cli/templates/static/codegen/src/eventFetching/SourceManager.res. */
let touch_all = () => {
  // Address.toString
  let _Address_toString = (Obj.magic(Address.toString): unit => unit)
  ignore(_Address_toString())
  // Promise.all
  let _Promise_all = (Obj.magic(Promise.all): unit => unit)
  ignore(_Promise_all())
  // Promise.race
  let _Promise_race = (Obj.magic(Promise.race): unit => unit)
  ignore(_Promise_race())
  // Promise.then
  let _Promise_then = (Obj.magic(Promise.then): unit => unit)
  ignore(_Promise_then())
  // Promise.thenResolve
  let _Promise_thenResolve = (Obj.magic(Promise.thenResolve): unit => unit)
  ignore(_Promise_thenResolve())
  // Utils.delay
  let _Utils_delay = (Obj.magic(Utils.delay): unit => unit)
  ignore(_Utils_delay())
}
let _ = touch_all()
