/* Auto-generated from scenarios/test_codegen/test/lib_tests/EntityHistory_test.res. */
let touch_all = () => {
  // EntityHistory.batchInsertRows
  let _EntityHistory_batchInsertRows = (Obj.magic(EntityHistory.batchInsertRows): unit => unit)
  ignore(_EntityHistory_batchInsertRows())
  // EntityHistory.fromTable
  let _EntityHistory_fromTable = (Obj.magic(EntityHistory.fromTable): unit => unit)
  ignore(_EntityHistory_fromTable())
  // EntityHistory.insertRow
  let _EntityHistory_insertRow = (Obj.magic(EntityHistory.insertRow): unit => unit)
  ignore(_EntityHistory_insertRow())
  // EntityHistory.makeHistoryRowSchema
  let _EntityHistory_makeHistoryRowSchema = (
    Obj.magic(EntityHistory.makeHistoryRowSchema): unit => unit
  )
  ignore(_EntityHistory_makeHistoryRowSchema())
  // Postgres.beginSql
  let _Postgres_beginSql = (Obj.magic(Postgres.beginSql): unit => unit)
  ignore(_Postgres_beginSql())
  // Postgres.unsafe
  let _Postgres_unsafe = (Obj.magic(Postgres.unsafe): unit => unit)
  ignore(_Postgres_unsafe())
  // Promise.thenResolve
  let _Promise_thenResolve = (Obj.magic(Promise.thenResolve): unit => unit)
  ignore(_Promise_thenResolve())
  // Table.mkField
  let _Table_mkField = (Obj.magic(Table.mkField): unit => unit)
  ignore(_Table_mkField())
  // Table.mkTable
  let _Table_mkTable = (Obj.magic(Table.mkTable): unit => unit)
  ignore(_Table_mkTable())
  // Utils.magic
  let _Utils_magic = (Obj.magic(Utils.magic): unit => unit)
  ignore(_Utils_magic())
}
let _ = touch_all()
